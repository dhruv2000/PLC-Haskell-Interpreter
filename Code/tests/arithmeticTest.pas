begin
    var a : real := 15.0;
    var b : real := 6.0;
    var c : real := a * b;
    var d : real := (7.0 + 2.0) * 3.0;
    var e : real := (a + 5.0) * 5.0;
    c := e / c;
    b := b * b + 5.0;
end
